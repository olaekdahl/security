# Copy to config.env and edit as needed

# AWS region to use for the demo
AWS_REGION=us-east-1

# Optional: set a specific bucket name (must be globally unique). If empty, scripts generate one.
BUCKET=

# Prefix in S3 where demo objects will be uploaded
S3_PREFIX=demo/

# Sampling percentage for Macie classification job (0-100). Use 100 for the demo.
SAMPLING_PERCENTAGE=100
